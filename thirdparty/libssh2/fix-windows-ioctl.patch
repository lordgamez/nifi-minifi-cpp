diff --git a/src/session.c b/src/session.c
index 212560b8..f539c52c 100644
--- a/src/session.c
+++ b/src/session.c
@@ -291,15 +291,15 @@ session_nonblock(libssh2_socket_t sockfd,   /* operate on this */
 #define SETBLOCK 1
 #endif
 
-#if defined(HAVE_FIONBIO) && (SETBLOCK == 0)
-    /* older unix versions and VMS*/
-    int flags;
-
-    flags = nonblock;
-    return ioctl(sockfd, FIONBIO, &flags);
-#undef SETBLOCK
-#define SETBLOCK 2
-#endif
+// #if defined(HAVE_FIONBIO) && (SETBLOCK == 0)
+//     /* older unix versions and VMS*/
+//     int flags;
+
+//     flags = nonblock;
+//     return ioctl(sockfd, FIONBIO, &flags);
+// #undef SETBLOCK
+// #define SETBLOCK 2
+// #endif
 
 #if defined(HAVE_IOCTLSOCKET) && (SETBLOCK == 0)
     /* Windows? */
